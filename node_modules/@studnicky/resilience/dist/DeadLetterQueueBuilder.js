/** Fluent builder for DeadLetterQueue. */
export class DeadLetterQueueBuilder {
    #create;
    #capacity;
    #clock;
    #signal;
    static create(create) {
        return new DeadLetterQueueBuilder(create);
    }
    constructor(create) {
        this.#create = create;
    }
    withCapacity(value) {
        this.#capacity = value;
        return this;
    }
    withClock(value) {
        this.#clock = value;
        return this;
    }
    withSignal(value) {
        this.#signal = value;
        return this;
    }
    build() {
        const options = {
            ...(this.#capacity !== undefined ? { 'capacity': this.#capacity } : {}),
            ...(this.#clock !== undefined ? { 'clock': this.#clock } : {}),
            ...(this.#signal !== undefined ? { 'signal': this.#signal } : {})
        };
        return this.#create(options);
    }
}
//# sourceMappingURL=DeadLetterQueueBuilder.js.map
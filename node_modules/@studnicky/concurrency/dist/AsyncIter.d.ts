/** Static utilities for async iterables: merge, filter, enrich. */
export declare class AsyncIter {
    /** FIFO merge of N async iterables in arrival order. */
    static merge<T>(...sources: AsyncIterable<T>[]): AsyncGenerator<T>;
    /** Yield items for which predicate returns true (sync or async). */
    static filter<T>(sourceIterator: AsyncIterable<T>, predicate: (item: T) => boolean | Promise<boolean>): AsyncGenerator<T>;
    /** Left-join enrichment: lookup per item; if non-null, merge item+enrichment; else yield item unchanged. */
    static enrich<T, E, R>(sourceIterator: AsyncIterable<T>, lookup: (item: T) => Promise<E | null>, merge: (item: T, enrichment: E) => R): AsyncGenerator<T | R>;
}
//# sourceMappingURL=AsyncIter.d.ts.map
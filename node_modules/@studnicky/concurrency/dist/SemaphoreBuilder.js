export class SemaphoreBuilder {
    static create(create) {
        const result = new SemaphoreBuilder(create);
        return result;
    }
    #create;
    #permits;
    constructor(create) {
        this.#create = create;
    }
    withPermits(permits) {
        this.#permits = permits;
        return this;
    }
    build() {
        const result = this.#create({ 'permits': this.#permits ?? 1 });
        return result;
    }
}
//# sourceMappingURL=SemaphoreBuilder.js.map
import { BaseError } from '@studnicky/errors';
/**
 * Abstract base for all file-lock errors.
 * Subclasses carry the specific code and context.
 */
export class FileLockError extends BaseError {
    constructor(args) {
        super(args);
    }
}
//# sourceMappingURL=FileLockError.js.map
export declare class ConfigValidation {
    /**
     * Extension seam — called by every assert method on validation failure.
     * Subclasses may `static override` to throw a domain-specific error instead.
     *
     * Fire-point: called with the constructed message string immediately before
     * the throw. `super.onValidationError` need not be called; the default throws
     * `ConfigurationError.create(message)`.
     */
    protected static onValidationError(message: string): never;
    static assertString(val: unknown, name: string): void;
    static assertNumber(val: unknown, name: string): void;
    static assertBoolean(val: unknown, name: string): void;
    static assertFunction(val: unknown, name: string): void;
    /**
     * Assert number is an integer.
     * Assumes assertNumber has already passed.
     */
    static assertInteger(val: unknown, name: string): void;
    /**
     * Assert number is finite (not Infinity or -Infinity).
     * Assumes assertNumber has already passed.
     */
    static assertFinite(val: unknown, name: string): void;
    /**
     * Assert number is non-negative (>= 0).
     * Assumes assertNumber has already passed.
     */
    static assertNonNegative(val: unknown, name: string): void;
    /**
     * Assert number is positive (> 0).
     * Assumes assertNumber has already passed.
     */
    static assertPositive(val: unknown, name: string): void;
    /**
     * Assert number is at least min value.
     * Assumes assertNumber has already passed.
     */
    static assertMin(val: unknown, min: number, name: string): void;
    /**
     * Assert number is positive or Infinity.
     * Assumes assertNumber has already passed.
     */
    static assertPositiveOrInfinity(val: unknown, name: string): void;
    static assertHasMethod(val: unknown, method: string, name: string): void;
    static assertFunctionOrObjectWithMethod(val: unknown, method: string, name: string): void;
    static assertNoUnknownKeys(config: Record<string, unknown>, knownKeys: Set<string>): void;
}
//# sourceMappingURL=configValidation.d.ts.map
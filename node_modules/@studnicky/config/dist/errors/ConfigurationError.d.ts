import { BaseError, type BaseErrorArgumentsType } from '@studnicky/errors';
/**
 * Configuration validation error. Extends `BaseError` so consumers get
 * `code`, `retryable`, `toJSON()`, and cause-chain helpers out of the box.
 *
 * Use `ConfigurationError.create(message)` to construct an instance.
 */
export declare class ConfigurationError extends BaseError {
    /** Fixed error code for all configuration validation failures. */
    static readonly errorCode = "config.invalid";
    /**
     * Factory — preferred construction path.
     * @param message - Human-readable description of the validation failure.
     * @param cause   - Optional underlying cause.
     */
    static create(message: string, cause?: unknown): ConfigurationError;
    protected constructor(args: Readonly<BaseErrorArgumentsType>);
}
//# sourceMappingURL=ConfigurationError.d.ts.map
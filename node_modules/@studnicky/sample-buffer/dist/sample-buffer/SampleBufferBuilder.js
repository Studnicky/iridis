/**
 * Fluent builder for creating SampleBuffer instances.
 *
 * @example
 * ```typescript
 * const buffer = SampleBuffer.builder().withCapacity(100).build();
 * ```
 */
export class SampleBufferBuilder {
    static create(create) {
        return new SampleBufferBuilder(create);
    }
    #create;
    #capacity;
    constructor(create) {
        this.#create = create;
    }
    withCapacity(value) {
        this.#capacity = value;
        return this;
    }
    build() {
        const options = { 'capacity': this.#capacity ?? 100 };
        const result = this.#create(options);
        return result;
    }
}
//# sourceMappingURL=SampleBufferBuilder.js.map
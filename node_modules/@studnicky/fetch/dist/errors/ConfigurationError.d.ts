/**
 * Error thrown when configuration validation fails
 *
 * @example
 * ```typescript
 * import { FetchClient, ConfigurationError } from '@studnicky/fetch';
 *
 * try {
 *   const client = FetchClient.create({
 *     baseURL: 'not-a-valid-url',
 *     timeout: -1000
 *   });
 * } catch (error) {
 *   if (error instanceof ConfigurationError) {
 *     console.error('Invalid configuration:', error.message);
 *   }
 * }
 * ```
 */
import { FetchBaseError } from './FetchBaseError.js';
export declare class ConfigurationError extends FetchBaseError {
    constructor(message: string);
}
//# sourceMappingURL=ConfigurationError.d.ts.map
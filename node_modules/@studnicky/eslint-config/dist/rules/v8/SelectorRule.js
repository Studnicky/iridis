export class SelectorRule {
    static create(ruleName, selector, message) {
        const create = (context) => {
            const listeners = {};
            const reportForbidden = (node) => {
                context.report({
                    'messageId': 'forbidden',
                    'node': node
                });
            };
            listeners[selector] = reportForbidden;
            return listeners;
        };
        return {
            'create': create,
            'meta': {
                'docs': {
                    'description': message,
                    'recommended': false
                },
                'messages': { 'forbidden': `${ruleName}: ${message}` },
                'schema': [],
                'type': 'problem'
            }
        };
    }
}
//# sourceMappingURL=SelectorRule.js.map
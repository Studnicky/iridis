/**
 * Fluent builder for constructing a `VirtualTimeCounter` instance via the create-closure idiom.
 */
export class VirtualTimeCounterBuilder {
    static create(create) {
        return new VirtualTimeCounterBuilder(create);
    }
    #create;
    #startMs;
    constructor(create) {
        this.#create = create;
    }
    withStartMs(value) {
        this.#startMs = value;
        return this;
    }
    build() {
        const options = {
            ...(this.#startMs !== undefined && { 'startMs': this.#startMs })
        };
        const result = this.#create(options);
        return result;
    }
}
//# sourceMappingURL=VirtualTimeCounterBuilder.js.map
import type { ErrorClassificationType } from '../../interfaces/ErrorClassificationType.js';
/**
 * Type guard for ErrorClassificationType
 *
 * Validates ErrorClassificationType structure and types.
 *
 * @param value - Value to check
 * @returns True if value is a valid ErrorClassificationType
 *
 * @example
 * ```typescript
 * if (isErrorClassification(result)) {
 *   if (result.retryable) {
 *     console.log('Error can be retried:', result.reason);
 *   }
 * }
 * ```
 */
export declare function isErrorClassification(value: unknown): value is ErrorClassificationType;
//# sourceMappingURL=isErrorClassification.d.ts.map
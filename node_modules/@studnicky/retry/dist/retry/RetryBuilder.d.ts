import type { ErrorClassifierInterface, RetryBuilderInterface, RetryConfigInterface, RetryInterface } from '../interfaces/index.js';
import type { ErrorClassifierFunctionType } from '../types/ErrorClassifierFunctionType.js';
/**
 * Builder for creating Retry instances with fluent API
 *
 * @example Basic usage
 * ```typescript
 * const retry = Retry.builder()
 *   .maxRetries(5)
 *   .build();
 * ```
 *
 * @example With errorClassifier
 * ```typescript
 * const retry = Retry.builder()
 *   .maxRetries(3)
 *   .errorClassifier(new DefaultHttpErrorClassifier())
 *   .build();
 * ```
 */
export declare class RetryBuilder<T extends RetryInterface = RetryInterface> implements RetryBuilderInterface<T> {
    #private;
    static create<T extends RetryInterface = RetryInterface>(create: (options: Partial<RetryConfigInterface>) => T): RetryBuilder<T>;
    private constructor();
    /**
     * Set error classifier for determining retry behavior
     */
    errorClassifier(value: ErrorClassifierFunctionType | ErrorClassifierInterface): this;
    /**
     * Set maximum number of retry attempts
     */
    maxRetries(value: number): this;
    /**
     * Build and return Retry instance
     * Config validation occurs in create()
     */
    build(): T;
}
//# sourceMappingURL=RetryBuilder.d.ts.map
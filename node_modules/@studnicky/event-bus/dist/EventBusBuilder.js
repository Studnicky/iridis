/** Fluent builder for creating EventBus instances. */
export class EventBusBuilder {
    static create(create) {
        const result = new EventBusBuilder(create);
        return result;
    }
    #create;
    constructor(create) {
        this.#create = create;
    }
    build() {
        const result = this.#create();
        return result;
    }
}
//# sourceMappingURL=EventBusBuilder.js.map
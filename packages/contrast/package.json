{
  "name": "@studnicky/iridis-contrast",
  "description": "WCAG 2.1 AA/AAA, APCA, and color-vision-deficiency enforcement — checks, corrects, and (for CVD) simulates against a resolved palette.",
  "version": "0.8.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test 'tests/**/*.test.ts'",
    "clean": "rm -rf dist",
    "prebuild": "rm -rf dist"
  },
  "dependencies": {
    "@studnicky/logger": "^4.0.0"
  },
  "peerDependencies": {
    "@studnicky/iridis": "^0.8.0"
  }
}

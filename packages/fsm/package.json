{
  "name": "@studnicky/iridis-fsm",
  "description": "Palette state machine — drives named-state transitions through iridis-anima's curve evaluation, with lifecycle hooks for enter/exit/reject.",
  "version": "0.8.1",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test 'tests/**/*.test.ts'",
    "clean": "rm -rf dist",
    "prebuild": "rm -rf dist"
  },
  "dependencies": {
    "@studnicky/iridis-algebra": "^0.8.1",
    "@studnicky/iridis-anima": "^0.8.1"
  },
  "peerDependencies": {
    "@studnicky/iridis": "^0.8.0"
  }
}

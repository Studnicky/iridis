{
  "name": "@studnicky/iridis-anima",
  "description": "Living-color animation engine — evaluates palette curves over time with easing, chromatic detours, and per-frame contrast enforcement.",
  "version": "0.9.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test 'tests/**/*.test.ts'",
    "clean": "rm -rf dist",
    "prebuild": "rm -rf dist"
  },
  "dependencies": {
    "@studnicky/iridis-algebra": "*",
    "@studnicky/iridis-contrast": "*"
  },
  "peerDependencies": {
    "@studnicky/iridis": "*"
  }
}

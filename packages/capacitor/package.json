{
  "name": "@studnicky/iridis-capacitor",
  "description": "Emits Capacitor status bar, splash screen, native theme config, and Android theme.xml from a resolved palette.",
  "version": "0.8.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types/index.ts"
  },
  "files": [
    "src"
  ],
  "dependencies": {
    "@studnicky/logger": "^4.0.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test 'tests/**/*.test.ts'",
    "clean": "rm -rf dist",
    "prebuild": "rm -rf dist"
  },
  "peerDependencies": {
    "@studnicky/iridis": "^0.8.0"
  }
}

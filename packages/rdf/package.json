{
  "name": "@studnicky/iridis-rdf",
  "description": "Emits the resolved palette as an RDF/OWL graph with SHACL-shaped reasoning annotations.",
  "version": "0.8.1",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test 'tests/**/*.test.ts'",
    "clean": "rm -rf dist",
    "prebuild": "rm -rf dist"
  },
  "peerDependencies": {
    "@studnicky/iridis": "^0.8.0"
  },
  "dependencies": {
    "@studnicky/logger": "^4.0.0",
    "n3": "^2.1.1"
  },
  "devDependencies": {
    "@types/n3": "^1.26.1",
    "typescript": "^6.0.0"
  }
}

{
  "name": "@studnicky/iridis-mui",
  "description": "Emits an MUI theme (createTheme() palette config) from resolved roles.",
  "version": "0.7.1",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "prebuild": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=24"
  },
  "peerDependencies": {
    "@studnicky/iridis": "^0.6.1"
  },
  "dependencies": {
    "@studnicky/logger": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^6.0.0"
  }
}

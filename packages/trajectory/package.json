{
  "name": "@studnicky/iridis-trajectory",
  "description": "Curated library of named palette trajectories — multi-stop curves through OKLCH×N space.",
  "version": "0.10.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test 'tests/**/*.test.ts'",
    "clean": "rm -rf dist",
    "prebuild": "rm -rf dist"
  },
  "dependencies": {
    "@studnicky/iridis-algebra": "*",
    "@studnicky/iridis-anima": "*"
  },
  "peerDependencies": {
    "@studnicky/iridis": "*"
  }
}

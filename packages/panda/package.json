{
  "name": "@studnicky/iridis-panda",
  "description": "Emits Panda CSS and UnoCSS theme configs from resolved roles.",
  "version": "0.10.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "prebuild": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=24"
  },
  "peerDependencies": {
    "@studnicky/iridis": "*"
  },
  "dependencies": {
    "@studnicky/logger": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^6.0.0"
  }
}

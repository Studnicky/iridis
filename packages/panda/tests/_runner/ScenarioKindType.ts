/**
 * Scenario classification used by `ScenarioInterface` and `ScenarioRunner`
 * to tag each row of a scenario-matrix table (`happy` | `edge` | `unhappy`).
 */
export type ScenarioKindType = 'happy' | 'edge' | 'unhappy';

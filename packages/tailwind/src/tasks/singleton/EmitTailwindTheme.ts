import { EmitTailwindTheme } from '../EmitTailwindTheme.ts';

/** Singleton instance registered as the `emit:tailwindTheme` pipeline task. */
export const emitTailwindTheme = new EmitTailwindTheme();

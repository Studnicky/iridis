{
  "name": "@studnicky/iridis-tailwind",
  "version": "0.2.0",
  "type": "module",
  "exports": {
    ".":       "./src/index.ts",
    "./types": "./src/types/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "prebuild":  "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test":      "node --import tsx --test 'tests/**/*.test.ts'",
    "clean":     "rm -rf dist"
  },
  "engines": {
    "node": ">=24"
  },
  "peerDependencies": {
    "@studnicky/iridis": "^0.3.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}

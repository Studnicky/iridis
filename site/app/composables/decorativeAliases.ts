/** Nuxt UI alias -> first-candidate source role, per Tokens.ts's own ALIAS_SOURCE — restricted to the decorative aliases AmbientBackground.vue's LAVA_ROLES actually reads. */
export const DECORATIVE_ALIASES: Record<string, string> = {
  'error':     'error',
  'info':      'info',
  'primary':   'brand',
  'secondary': 'accent-alt',
  'success':   'success',
  'warning':   'warning'
};
